import tkinter as tk
import csv
import os

window = tk.Tk()
window.title("UniMak Library Management System")
window.geometry("700x500")

titleL = tk.Label(window, text="UniMak Library Management", font=("Monterrat", 20.7))
titleL.grid(row=0, column=1, padx=10, pady=10)
